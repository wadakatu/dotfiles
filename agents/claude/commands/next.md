---
description: 現在の PR のマージを確認し、default ブランチを同期して、計画中の次のタスクへ進む。「PRをマージしました 次に進んでください」の定型フローを1コマンド化。
---

# /next

マージ確認 → default ブランチ同期 → 次タスク着手、のキュー進行を行う。

## 処理フロー

1. **現在の PR のマージ確認**
    - `git branch --show-current` で現ブランチを確認
    - `gh pr view --json state,mergedAt,url` で現ブランチの PR の状態を確認
    - マージ済みでなければ**ここで停止**し、PR の現状（open / CI / レビュー状態）を1〜3行で報告する
    - 現ブランチに PR がない場合（すでに default ブランチ上にいる等）は手順 3 へ進む

2. **default ブランチの同期とローカル掃除**
    - default ブランチ（`gh repo view --json defaultBranchRef` で確認）へ switch → `git pull --ff-only`
    - マージ済みの作業ブランチを `git branch -d` で削除する（`-D` は使わない。削除できない場合は理由を報告して残す）

3. **次タスクの特定と着手**
    - 次タスクは以下の優先順で特定する:
        1. 会話中で合意済みの計画・タスクリストの次項目
        2. エピック issue / tracking issue のチェックリストの未完了項目
        3. ユーザーの直前の指示
    - 特定できたら **「次: 〜」と1〜2行で提示してから**着手する
    - 候補が複数ある・特定できない場合は、候補を並べてユーザーに確認する（勝手に選ばない）
    - 着手時は通常のフロー（ブランチ作成 → 実装 → 検証 → `/pr`）に従う

## 注意事項

- マージ確認を省略しない。マージ前に default ブランチを同期して次を始めると、後続 PR の base が混線する
- squash merge の場合 `git branch -d` が拒否されることがある。その場合はリモート追跡が消えている（マージ済みの）ことを `gh pr view` で確認したうえで削除してよい
